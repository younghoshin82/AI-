# DESIGN.md - AI 기반 투자계획서 검증 시스템 설계서

> 근거 문서: `PRD.md`(목적/기능/사용자), `CLAUDE.md`(AI 역할/원칙/검증등급)
> 대상 데이터: `AI_Investment_Validation_Sample_Pack/Project_01 ~ Project_05`
> 본 문서는 설계 단계 산출물이며, 구현 코드는 포함하지 않는다.

---

## 1. 요구사항 및 사용자 업무 흐름 분석

### 1.1 기능 요구사항

PRD의 주요기능 7개를 시스템 기능 요구사항(FR)으로 분해한다.

| ID | 기능 요구사항 | 근거(PRD/CLAUDE) | 비고 |
|----|---------------|------------------|------|
| FR-01 | 투자자료 업로드 및 투자건 등록 | PRD 2-1 | 샘플 폴더 선택 + 파일 업로드 2가지 경로 |
| FR-02 | PDF 텍스트/수치 추출 | PRD 2-2, CLAUDE 1 | 페이지 번호를 출처로 보관 |
| FR-03 | Excel 시트/셀 값 추출 | PRD 2-2, CLAUDE 1 | 시트명·셀주소를 출처로 보관 |
| FR-04 | 투자비 정합성 검증 | PRD 2-3, CLAUDE 2 | 계획서 총투자비 vs 내역 합계 |
| FR-05 | 경제성 지표·가정 검증 | PRD 2-4, CLAUDE 3 | NPV / IRR / WACC / 회수기간 |
| FR-06 | 기대효과 정합성·중복 검증 | PRD 2-5, CLAUDE 4 | 세부효과 합계 vs 계획서 기재값 |
| FR-07 | 리스크 도출 | PRD 2-6, CLAUDE 5 | LLM 기반, AI 의견으로 표기 |
| FR-08 | 투자검토요약 생성 및 내보내기 | PRD 2-7, CLAUDE 6 | 마크다운 초안 + 다운로드 |

### 1.2 비기능 요구사항 (CLAUDE.md 원칙의 시스템 반영)

| ID | 원칙 | 설계 반영 방식 |
|----|------|----------------|
| NFR-01 | 자료에 없는 수치는 추정하지 않는다 | 추출 실패 항목은 공란이 아닌 `미확인`으로 표시하고 등급 `확인필요` 부여. 파서와 LLM 모두 값 보간을 금지한다 |
| NFR-02 | AI의 의견과 사실을 구분한다 | 화면과 리포트에서 `[사실]`(추출값·룰 판정)과 `[AI 의견]`(LLM 생성 문구)을 라벨로 분리한다 |
| NFR-03 | 투자 승인/부결을 판단하지 않는다 | 산출물에 승인·부결·권고 표현을 쓰지 않고, 검증 등급과 확인사항만 제시한다 |
| NFR-04 | 모든 결과에 근거를 표시한다 | 모든 추출값에 출처(파일명 + 시트/페이지)를 동반하고, 룰 결과에는 비교값과 계산식을 함께 표시한다 |

### 1.3 사용자 및 업무 흐름

**사용자**: 투자기획 담당자 — 투자기획팀 심사 담당자 관점에서 제출 자료를 1차 검토한다.

**As-Is (수작업)**

1. 투자계획서 PDF와 첨부 Excel을 각각 열어 수치를 눈으로 대조
2. 총투자비와 기대효과 합계를 수동으로 재계산
3. 누락 지표와 불일치 항목을 메모
4. 현업 질의사항을 정리하고 투자검토요약을 직접 작성

**To-Be (본 시스템)**

```
[1] 투자건 등록        자료 선택 / 업로드
        ↓
[2] 정보 추출          PDF·Excel → 필수 7항목 + 출처
        ↓
[3] 룰 기반 검증       투자비 / 경제성 / 기대효과 → 등급 판정
        ↓
[4] LLM 분석           리스크 · 추가확인사항 · 현업 질문사항 도출
        ↓
[5] 검토요약 생성      7개 항목 마크다운 초안 → 다운로드
        ↓
[6] 담당자 최종 확인   근거를 보며 수정 후 내부 보고 자료로 사용
```

핵심 설계 방향은 **시스템이 판단을 대신하지 않고, 담당자가 확인할 대상을 좁혀준다**는 것이다.

### 1.4 검증 시나리오 (샘플 데이터 기준)

샘플팩의 5개 프로젝트는 각각 다른 결함 유형을 담고 있어 룰 엔진 테스트 케이스로 그대로 사용한다.

| 프로젝트 | 결함 유형 | 기대 검증 결과 |
|----------|-----------|----------------|
| Project_01_Normal | 정상 | 전 항목 `정상` |
| Project_02_CAPEX_Mismatch | 총투자비와 투자비 내역 합계 불일치 | 투자비 검증 `높음` |
| Project_03_Effect_Mismatch | 기대효과 합계 불일치 | 기대효과 검증 `높음` |
| Project_04_Missing_Economics | 경제성 지표 누락 | 경제성 검증 `확인필요` |
| Project_05_Timing_Overlap | 효과 발생시점 중복 | 기대효과 중복 `주의` |

각 프로젝트의 `05_투자검토요약_정답본.docx`는 산출물 품질 비교용 참조본으로만 사용하며, 검증 로직의 입력으로는 쓰지 않는다.

---

## 2. Streamlit 기반 시스템 및 멀티페이지 화면 설계

### 2.1 시스템 구성

```
┌──────────────────────────────────────────────────┐
│  Presentation : Streamlit Multipage UI (5 pages) │
├──────────────────────────────────────────────────┤
│  Service      : 추출 오케스트레이션 / 검증 실행 /  │
│                 요약 생성                         │
├──────────────────────────────────────────────────┤
│  Core         : PDF Parser · Excel Parser ·      │
│                 Rule Engine · LLM Client         │
├──────────────────────────────────────────────────┤
│  Data         : Sample Pack (PDF/XLSX) ·         │
│                 st.session_state · output/       │
└──────────────────────────────────────────────────┘
```

- **UI 프레임워크**: Streamlit 멀티페이지(`pages/` 디렉터리) 구조
- **상태 관리**: `st.session_state`로 페이지 간 데이터 전달
- **성능**: 파싱 결과는 `@st.cache_data`로 캐싱하여 페이지 이동 시 재파싱을 방지
- **LLM**: Claude API(`claude-opus-5`, adaptive thinking, 구조화 출력) 사용, 자격 증명 미설정·호출 실패 시 룰 판정에서 조립한 규칙 기반 템플릿으로 자동 대체

### 2.2 화면 구성 (사이드바 5개 메뉴)

사이드바 상단에 **프로젝트 선택 박스**를 상시 배치하여 모든 페이지가 동일한 선택 프로젝트를 참조한다.

| # | 페이지 | 목적 | 주요 구성 요소 |
|---|--------|------|----------------|
| 1 | 대시보드 | 전체 투자건 현황 파악 | 프로젝트 카드 목록, 등급 요약 지표, 진행 상태 |
| 2 | 투자건 등록 | 검증 대상 자료 확정 | 샘플 프로젝트 선택 / 파일 업로더, 파일 목록 및 문서 유형 |
| 3 | 정보추출 결과 | 추출값과 출처 확인 | 필수 7항목 추출 표(값 + 출처), 원본 상세 테이블 |
| 4 | AI 검증 결과 | 룰 기반 검증 결과 확인 | 룰별 결과 카드, 등급 색상 배지, 비교값·계산식 |
| 5 | 투자검토 요약 | 최종 산출물 생성 | LLM 분석 결과, 7항목 마크다운 초안, 다운로드 버튼 |

### 2.3 페이지별 상세 설계

#### 1. 대시보드

- 상단: 전체 투자건 수와 등급별 건수(높음 / 주의 / 확인필요 / 정상)를 한 줄에 균등 배치한 지표 카드.
  `st.columns` 는 내용 길이에 따라 카드 폭이 흔들리므로 CSS flex(`flex:1 1 0`)로 같은 너비를 보장하고,
  좁은 화면에서는 자동 줄바꿈시킨다. 같은 컴포넌트를 4번 페이지 등급 종합에도 재사용한다
- 본문: 프로젝트별 요약 테이블 — 투자명, 총투자비, NPV, IRR, 최고 위험등급, 처리 상태
- 테이블과 사이드바 선택을 양방향 동기화한다. 표에서 행을 클릭하면 `selected_project` 를 갱신하고
  사이드바 selectbox 위젯 키를 지워 새 값으로 다시 만들며, 반대로 사이드바에서 투자건을 바꾸면
  표에 남은 행 선택 표시를 해제한다. 두 경우를 구분하기 위해 마지막 선택 행을 `dashboard_last_row` 에 보관한다

#### 2. 투자건 등록

- 데이터 소스 선택: `샘플 프로젝트 사용` / `직접 업로드`
- 샘플 선택 시 Project_01 ~ Project_05 목록을 표시하고, 선택하면 해당 폴더의 파일 목록을 표시
- 파일 목록 테이블: 파일명, 확장자, 문서 유형(투자계획서 / 경제성검토 / 투자비내역 / 기대효과산출), 파일 크기
- `정보 추출 실행` 버튼을 누르면 추출을 수행하고 3번 페이지로 안내

#### 3. 정보추출 결과

필수 7항목을 표 형태로 표시하며, 출처를 항상 함께 보여준다.

| 항목 | 값 | 단위 | 출처 파일 | 위치 |
|------|----|------|-----------|------|
| 투자명 | … | - | 01_투자계획서.pdf | p.1 |
| 총 투자비 | … | 억원 | 01_투자계획서.pdf | p.2 |
| NPV | … | 억원 | 02_경제성검토.xlsx | 기본가정!B9 |
| IRR | … | % | 02_경제성검토.xlsx | 기본가정!B10 |
| WACC | … | % | 02_경제성검토.xlsx | 기본가정!B7 |
| 회수기간 | … | 년 | 02_경제성검토.xlsx | 기본가정!B11 |
| 기대효과 | … | 억원/년 | 01_투자계획서.pdf | p.2 |

항목별 1순위 출처는 투자계획서(투자명·총투자비·기대효과)와 경제성검토 Excel(NPV·IRR·WACC·회수기간)로 두고,
1순위에 값이 없으면 2순위 문서를 조회한다. 채택하지 않은 출처와는 값을 교차 검증해 불일치를 함께 제시한다.

- 추출 실패 항목은 `미확인`으로 표시하고 별도 경고 영역에 모아 안내한다 (NFR-01)
- 하단 확장 영역에 투자비 내역 상세와 기대효과 세부 항목의 원본 테이블을 함께 제공한다

#### 4. AI 검증 결과

| 룰 ID | 검증 내용 | 판정 기준 | 불충족 시 등급 |
|-------|-----------|-----------|----------------|
| R-01 | 투자비 정합성 | 계획서 총투자비 = 투자비 내역 합계 (허용오차 max(0.05억원, 0.1%)), 경제성 적용 투자비도 부가 비교 | 높음 |
| R-02 | 경제성 지표 존재 | NPV·IRR·WACC·회수기간 4개 항목이 모두 존재 | 확인필요 |
| R-03 | 기대효과 정합성 | 세부효과 합계 = 계획서 기재 효과 | 높음 |
| R-04 | 기대효과 중복 | ① 효과 반영 시점 < 가동 예정 시점 ② 같은 성격의 효과 항목 2건 이상 계상 | 주의 |

- 표시 방식: 룰별 카드에 등급 색상 띠와 배지 — 높음 빨강(`#D32F2F`), 주의 주황(`#EF6C00`), 확인필요 노랑-주황(`#F9A825`), 정상 초록(`#388E3C`). 심각도 순서와 색의 진하기를 일치시킨다
- 화면 문자열은 모두 한국어로 두고, 널리 쓰이는 영어 제품명은 `엑셀 (Excel)` 처럼 병기한다. 직접 삽입하는 HTML에는 `translate="no"` 를 붙여 브라우저 자동 번역이 한글 라벨을 재번역하지 않게 한다
- 각 카드에 비교에 사용한 값, 차이, 출처를 함께 표기한다 (NFR-04)
- 판정은 등급 부여까지만 수행하고 승인·부결 문구는 출력하지 않는다 (NFR-03)

#### 5. 투자검토 요약

- LLM 생성 영역: 리스크 / 추가확인사항 / 현업 질문사항 / 투자검토 코멘트 — 모두 `[AI 의견]` 라벨을 부착
- 최종 초안 구성

```
1. 투자개요          추출값 기반            [사실]
2. 투자비 검토        R-01 결과 + 근거       [사실]
3. 경제성 검토        R-02 결과 + 지표값     [사실]
4. 기대효과 검토      R-03 / R-04 결과       [사실]
5. 주요 리스크        LLM 도출              [AI 의견]
6. 현업 추가 확인사항  LLM 도출              [AI 의견]
7. 종합 의견          검증 등급 종합 + 코멘트 [AI 의견]
```

- 다운로드: 마크다운(.md) / 텍스트(.txt) / PDF

### 2.4 세션 상태 키 설계

| 키 | 타입 | 용도 |
|----|------|------|
| `selected_project` | str | 선택된 프로젝트 폴더명 |
| `project_files` | list | 선택 프로젝트의 파일 메타 목록 |
| `extracted_data` | dict | 항목별 `{value, unit, source_file, source_loc}` |
| `validation_results` | list | 룰별 `{rule_id, grade, message, evidence}` |
| `llm_analysis` | dict | 리스크 · 추가확인사항 · 질문사항 · 코멘트 |
| `summary_markdown` | str | 최종 검토요약 초안 |

---

## 3. 폴더 구조 및 개발 로드맵

### 3.1 폴더 구조 (계획)

```
[현대제철_AI에이전트]_신영호_최종결과물/
├── PRD.md                          # 제품 요구사항
├── CLAUDE.md                       # AI 역할·원칙·검증등급
├── DESIGN.md                       # 본 설계서
├── requirements.txt                # 의존 패키지
├── app.py                          # Streamlit 엔트리
├── pages/
│   ├── 1_대시보드.py
│   ├── 2_투자건_등록.py
│   ├── 3_정보추출_결과.py
│   ├── 4_AI_검증_결과.py
│   └── 5_투자검토_요약.py
├── src/
│   ├── config.py                   # 경로·상수·등급 정의
│   ├── data_loader.py              # 샘플팩 스캔, 프로젝트/파일 목록
│   ├── parsers/
│   │   ├── base.py                 # ParsedValue·SourceRef 등 공통 자료구조
│   │   ├── pdf_parser.py           # 투자계획서 추출 (페이지 출처 포함)
│   │   ├── excel_parser.py         # 경제성·투자비·기대효과 추출
│   │   └── extractor.py            # 필수 7항목 통합 추출
│   ├── validators/
│   │   └── rule_engine.py          # R-01 ~ R-04
│   ├── llm/
│   │   ├── client.py               # Claude API 호출 + 규칙 기반 폴백
│   │   └── prompts.py              # CLAUDE.md 원칙을 주입한 프롬프트
│   ├── report/
│   │   ├── summary_builder.py      # 7항목 마크다운 조립
│   │   └── exporter.py             # md / txt / pdf 내보내기
│   └── ui/
│       └── components.py           # 등급 배지, 출처 표기 등 공통 위젯
├── AI_Investment_Validation_Sample_Pack/   # 입력 샘플 데이터
└── output/                         # 생성된 검토요약 저장
```

### 3.2 개발 로드맵

| 단계 | 범위 | 산출물 | 완료 기준 |
|------|------|--------|-----------|
| Phase 0 | 문서화 | PRD.md, CLAUDE.md, DESIGN.md | 설계 확정 **(완료)** |
| Phase 1 | UI 뼈대 | app.py, pages/ 5개, data_loader | 사이드바 5개 메뉴 동작, Project_01~05 선택 시 파일명 목록 표시 **(완료)** |
| Phase 2 | 파서 | pdf_parser, excel_parser, extractor | 필수 7항목과 출처가 3번 페이지에 표로 표시 **(완료)** |
| Phase 3 | 룰 엔진 | rule_engine, 등급 배지 | R-01~R-04 동작, 5개 샘플이 1.4절 기대 결과와 일치 **(완료)** |
| Phase 4 | LLM 요약 | llm/, summary_builder, exporter | 7항목 초안 생성, md/txt/pdf 다운로드 동작 **(완료)** |
| Phase 5 | 검증·정리 | 테스트, README | 정답본 대비 산출물 비교, NFR-01~04 준수 점검 |

### 3.3 기술 스택 (예정)

| 구분 | 선택 | 사유 |
|------|------|------|
| UI | Streamlit | 멀티페이지·파일 업로더·표 렌더링을 최소 코드로 구현 |
| PDF 파싱 | pdfplumber | 페이지 단위 텍스트·표 추출로 출처 표기에 유리 |
| Excel 파싱 | openpyxl / pandas | 시트·셀 주소 추적 및 표 형태 처리 |
| LLM | Claude API (`claude-opus-5`) | 한국어 문서 요약 품질과 원칙 기반 지시 준수 |
| PDF 출력 | reportlab | 한글 폰트 임베딩 가능 |

### 3.4 구현 전 확인 필요 사항

- ~~총투자비 단위 정규화 규칙 확정~~ → Phase 2에서 확인: 금액은 전 프로젝트 `억원` 단위로 통일되어 있고, WACC·IRR만 Excel에 비율(0.08)로 저장되어 있어 `%` 단위로 정규화한다
- ~~투자비 정합성 허용오차 조정~~ → Phase 3에서 `max(0.05억원, 기준값의 0.1%)`로 확정. 세부 항목 합계는 투자계획서(PDF) 기재 항목을 기준으로 하며, 샘플팩 Excel 내역표는 합계·차이 행이 항목 행 자리를 차지해 항목이 누락되어 있어 합계 기준으로 쓰지 않고 자료 품질 메모로만 표시한다
- ~~Claude API 키가 없는 환경의 시연 방식~~ → Phase 4에서 두 경로를 모두 구현. 자격 증명이 있으면 `claude-opus-5` 호출, 없거나 호출이 실패하면 룰 판정 결과에서 조립한 규칙 기반 템플릿으로 대체하고 생성 엔진을 화면·리포트에 명시한다
